'use strict';

// Imports
var getFilenames = require('./getFilenames.js');
var processFiles = require('./processFiles.js');

// Exports
module.exports = {
  getFilenames: getFilenames,
  processFiles: processFiles
};